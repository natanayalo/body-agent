[pytest]
testpaths = services/api/tests
asyncio_mode = strict
addopts =
  --strict-markers
  --doctest-modules
  --cov=app
  --cov-report=term-missing
  --cov-report=xml
  --cov-branch
  --cov-fail-under=80
  --ignore=app/data
